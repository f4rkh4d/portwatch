# Usage

Run `portwatch --help` to see all available commands and flags.

## Common workflows

See the project README for a quick tour. This document collects longer
end-to-end recipes that do not fit in the README.

## Exit codes

- `0` — success
- `1` — expected failure (e.g. validation mismatch)
- `2` — unexpected error
